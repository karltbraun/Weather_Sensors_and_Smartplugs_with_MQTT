# Where we are now - 2025-11-24

We can update local_sensors via publication to KTBMES/ROSA/sensors/config/local_sensors
We have tested this from the command line.
Next step is to publish to an image and test in docker.
